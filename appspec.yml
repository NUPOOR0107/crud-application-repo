version: 0.0
os: linux
files:
  - source: /
hooks:
  BeforeInstall:
    - location: install_dependencies.sh
      timeout: 300
  ApplicationStart:
    - location: start_application.sh
      timeout: 300
      runas: root
